agent:
  id: "epamdemotest"
  team: "platform-eng"
  runtime: aks
  interaction_model: "standalone"
  guardrail_profile: "moderate"

identity:
  sp_client_id: "1d41f2df-b7fe-4596-a5b0-ee6e23d4e118"
  keyvault_url: "https://kv-agenticdemo-poc.vault.azure.net/"

tools: []

evaluation:
  suite: default-strict
  ci_gate: true
  pass_threshold: 0.95
  datasets:
    - id: golden-dataset
      version: v1.0
      source: repo
      path: evals/datasets/golden-dataset.json

output_policy:
  violation_threshold: 4
  window_minutes: 1
  action: block
  notify_webhook: 

prompts:
  active_version: v1.0
  source: prompts/system.txt
